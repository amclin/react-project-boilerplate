// This file is here to ensure that Jest can parse files properly
// for running unit tests of the templates from the generator
// project. This is EXPLICITLY different from the babel needs of
// of the generated projects themselves. That comes from the
// babelrc file within the template.
{
  "presets": [
    [
      "next/babel", 
      {
        // This tells the preset to look for browserslist in package.json
        // See: https://babeljs.io/docs/en/babel-preset-env#browserslist-integration
        "useBuiltIns": "entry"
      }
    ]
  ]
}
